openapi: 3.1.0
info:
  version: 1.0.0
  title: ConstactList
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    Constact List Server
tags:
  - name: Auth
    description: auth orepation
  - name: Contacts
    description: Contacts orepation
servers:
  - url: http://localhost:3000
  # - url: #посилання на задеплоєний сайт
paths:
  /auth/register:
    post:
      $ref: ../swagger/paths/auth/register/post.yaml
  /auth/login:
    post:
      $ref: ../swagger/paths/auth/login/post.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
